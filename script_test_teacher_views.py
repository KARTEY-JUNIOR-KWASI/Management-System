import os, django; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'school_management.settings'); django.setup(); from django.test import Client; from accounts.models import User; c = Client(); u = User.objects.filter(role='teacher').first(); c.force_login(u); response = c.get('/teachers/dashboard/'); print('Dashboard:', response.status_code); response = c.get('/teachers/attendance/'); print('Attendance:', response.status_code); response = c.get('/teachers/grades/'); print('Grades:', response.status_code);
