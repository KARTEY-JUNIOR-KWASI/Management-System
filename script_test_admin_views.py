import os, django; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'school_management.settings'); django.setup(); from django.test import Client; from accounts.models import User; c = Client(); admin = User.objects.filter(role='admin').first(); c.force_login(admin); response = c.get('/admin_dashboard/students/create/'); print('Add Student GET:', response.status_code); response = c.get('/analytics/'); print('Analytics Dashboard:', response.status_code);
